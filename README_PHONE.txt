PHONE VERSION
This version avoids better-sqlite3/native compilation and stores catalog data in data.json, making it easier to run in Termux on Android.
1) npm install
2) create .env (see .env.example)
3) npm start
4) open http://localhost:3000
5) admin: http://localhost:3000/admin.html
Only distribute files/content you have permission to distribute.
